/**
 * x402 Intent Firewall - Backend Entry Point
 * 
 * Runs the demo to show the firewall in action with test scenarios
 */

// Import and run the demo - the demo file has main() that runs automatically
import "./demo";
